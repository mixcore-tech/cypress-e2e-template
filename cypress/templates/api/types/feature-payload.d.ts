/**
 * FILL-IN TEMPLATE — the request body of your feature's create/update endpoint.
 * Copy the real shape from your API's swagger page or the browser network tab.
 */
export interface IFeaturePayload {
    name: string
    description: string
    // TODO(template): the rest of your request fields
}
