/**
 * FILL-IN TEMPLATE — the response body your API returns for a created/fetched
 * feature. The `id` field is what entity helpers track for cleanup.
 */
export interface IFeatureResponse {
    id: number
    name: string
    description: string
    // TODO(template): the rest of your response fields
}
